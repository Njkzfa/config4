<# Конфигурация базы данных #>
default_port: 5432;

{
    database = {
        type = "postgresql",
        host = "localhost",
        port = [default_port],
        name = "mydb",
        pool = {
            size = 10,
            timeout = 5
        },
        tables = (list users products orders logs)
    }
}