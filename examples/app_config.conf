<# Конфигурация приложения #>
app_version: 2;
debug_mode: false;

{
    application = {
        name = "MyApp",
        version = [app_version],
        debug = [debug_mode],
        features = (list authentication api websocket caching),
        limits = {
            max_requests = 1000,
            memory = "512MB"
        }
    }
}